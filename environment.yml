# quickstudy 后端环境（conda 管理 Python，pip 装项目依赖——多数包仅 PyPI 有）
# 用法：conda env create -f environment.yml && conda activate quickstudy
name: quickstudy
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - -e ".[dev,m2,render,web]"
